import RequestHeader from "../../RequestHeader";

const MESSAGE_NAME = "ValidationMLotReserved";

export default class ValidationMLotReservedRequestHeader extends RequestHeader{

    constructor() {
        super(MESSAGE_NAME);
    }
    
}